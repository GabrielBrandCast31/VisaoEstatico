/* Configuração do site — equivalente às variáveis NEXT_PUBLIC_* do .env.
   Edite aqui antes de publicar. */
window.VISAO_CONFIG = {
  // Webhook do Google Apps Script (planilha + e-mail). Vazio = envio desativado.
  WEBHOOK_URL: "",
  // Link de agendamento do Google Calendar.
  BOOKING_URL: "https://calendar.app.google/PLACEHOLDER",
  // WhatsApp comercial (formato internacional, sem +).
  WHATSAPP_NUMBER: "5521997079059",
  // Analytics (deixe em branco para desabilitar).
  META_PIXEL_ID: "",
  GTM_ID: "",
};
